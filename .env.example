# No backend or secret environment variables are required for the local-only PWA scaffold.
# Add public build-time settings here later if needed. Never place encryption keys or passwords in VITE_* variables.
